---
status: passed
milestone: v1.1
date: 2026-06-19
---

# Milestone v1.1 Audit — Dynamic Config + Security Hardening

## Result: PASSED (offline) — live re-verify deferred

3 phases complete (6, 7, 8), 13/13 requirements done. 318 tests passing, 2 skipped (live), `tsc --noEmit` clean. All merged to main + pushed.

## Phases

| Phase | Name | Tests | Verify | Review fixes |
|-------|------|-------|--------|--------------|
| 6 | Dynamic Config from ClickUp | 291 | human_needed (live) | WR-01 empty-fetch poisons fallback |
| 7 | Security Audit (OWASP Top 10) | — (report) | passed | n/a (audit only) |
| 8 | Security Hardening | 318 | human_needed (live) | WR-01 GET-before-gate, WR-02 taskId hyphens |

## Requirements coverage: 13/13

DYN-01..06 (Phase 6) · SEC-01..03 (Phase 7) · SEC-04..07 (Phase 8) — all Done.

## What shipped

- **Dynamic config:** Cliente options + ClickUp members read live (Redis-cached, TTL ~10min), Slack→ClickUp resolved by email, resilient 3-tier fallback (hot cache → last-good → static maps), secret-gated manual refresh. Adding a client/member in ClickUp no longer needs a redeploy. Static maps kept as the safety net + curated-alias overlay.
- **Security audit:** full OWASP Top 10 (2021) review → SECURITY.md. 0 critical, 2 high, 4 medium, 5 low. 0 runtime dependency vulns (21 advisories dev-only).
- **Security hardening:** ops endpoints (diag, refresh-config) fail-closed (404 when OPS_API_TOKEN unset), Bearer-token gated (no more secret-in-URL), POST for mutations, diag self-join restricted to the configured channel, reduced disclosure, outbound Slack mrkdwn escaped in the preview, taskId path validated. SECURITY.md findings closed/accepted with rationale.

## Security posture (SECURITY.md closure)

- **Fixed:** FIND-01 (secret-in-URL → Bearer OPS_API_TOKEN), FIND-02 (state-over-GET + arbitrary self-join), FIND-03 (topology disclosure), FIND-07 (preview mrkdwn injection), FIND-11 (taskId validation).
- **Accepted (rationale in SECURITY.md):** FIND-04 (no signed webhook timestamp; 24h dedup is the bound), FIND-05 (0 runtime vulns; dev-only deps), FIND-06 (rate-limiting deferred), FIND-08 (kill-switch fail-open by design), FIND-09 (64-bit dedup key), FIND-10 (ops audit trail deferred).

## New live-setup steps (post-deploy)

1. **Slack scope `users:read.email`** + reinstall — enables email-based Slack→ClickUp assignee resolution (DYN-04). Degrades to name/alias + static map if absent.
2. **`OPS_API_TOKEN`** env var (optional) — set a random 32+ char value to enable the ops endpoints; leave unset and they 404. Access via `Authorization: Bearer`, never `?secret=`.
3. Re-verify ops endpoints live: unset → 404; set + wrong/no Bearer → 401; correct Bearer GET diag → reduced report; POST refresh-config → {clearedCount}; GET refresh-config → 405.

## Verdict

v1.1 code complete and shippable. Sync the fork + redeploy to pick up dynamic config + hardening. The two live-setup steps above are the only operator actions.
