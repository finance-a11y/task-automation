---
status: passed
milestone: v1.0
date: 2026-06-18
---

# Milestone v1.0 Audit — Slack → ClickUp Task Bot

## Result: PASSED (offline) — live deploy steps human-deferred

All 5 phases complete, all 23 v1 requirements implemented and verified at the code/test level. Cross-phase integration audited: both flows wired end-to-end, no gaps.

## Phases

| Phase | Name | Tests | Verify | Review fixes |
|-------|------|-------|--------|--------------|
| 1 | Serverless Foundation | 35 | human_needed (live) | HIGH dedup-drop + 2 LOW |
| 2 | NL Parser + Resolver | 87 | passed | CRIT prototype-pollution + date bugs |
| 3 | Confirm + Create (Flow A) | 136 | human_needed (live) | BLOCKER duplicate-create + 4 WR |
| 4 | Reverse Notifications (Flow B) | 186 | human_needed (live) | mrkdwn-injection + dedup collision |
| 5 | Hardening | 233 | human_needed (live) | POST-retry double-create + Retry-After clamp |

Final suite: **233 passing, 1 skipped (live OpenAI smoke), `tsc --noEmit` clean.**

## Requirements coverage: 23/23

INGEST-01..04 · PARSE-01..04 · CONFIRM-01..05 · CREATE-01..04 · NOTIFY-01..03 · HARD-01..03 — all Complete.

## Integration (cross-phase, code-level)

- Flow A: events → app → process (kill-switch → dedup → filter → parse+resolve → pending → preview) → interactions (claim → createTask via retry → task↔thread map → link back). WIRED.
- Flow B: clickup webhook → X-Signature verify → parse/filter → task↔thread lookup → escaped Spanish notification to thread. WIRED.
- Redis namespaces consistent (`evt:`, `whk:`, `pending:`, `task2thread:`, `killswitch:`), all via store/redis.ts helpers.
- Id chain coherent: createTask().id → task2thread → webhook getThreadForTask.
- Env schema complete (12 vars), no orphans.

## Human-deferred (live deploy — required before production use)

These cannot be exercised without live services and are the remaining setup the team must do:

1. **Slack app** — create app, Event Subscriptions Request URL → `/api/slack/events`, Interactivity Request URL → `/api/slack/interactions`, scopes (chat:write, channels read), install → capture `SLACK_BOT_TOKEN` + `SLACK_SIGNING_SECRET`; set `SLACK_TASK_CHANNEL_ID` to the dedicated channel; add the bot's own user id handling.
2. **Vercel** — deploy, enable **Fluid Compute**, set all env vars.
3. **Upstash Redis** — provision via Vercel Marketplace → `UPSTASH_REDIS_REST_URL` + `_TOKEN`.
4. **OpenAI** — `OPENAI_API_KEY` (+ optional `OPENAI_MODEL`, default gpt-4o-mini).
5. **ClickUp** — `CLICKUP_API_TOKEN`; run `scripts/register-clickup-webhook.mjs` post-deploy → capture `CLICKUP_WEBHOOK_SECRET`; verify live X-Signature wire format matches the (defensively built) verifier.
6. **Live confirmations** — ACK<3s on a real event; preview→confirm creates a real task in Task-Seo Team with correct cliente/asignados/fechas/link; status/assignee change posts back to the thread; kill-switch flip halts the bot.

## Verdict

Code milestone complete and shippable pending the live provisioning above. Recommend completing Slack+Vercel+Upstash+ClickUp setup, then running the Phase 1/3/4/5 live checklists in the per-phase VERIFICATION.md files.
