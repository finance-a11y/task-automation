---
phase: 08-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/env.ts
  - src/config/env.test.ts
  - src/ops/auth.ts
  - src/ops/auth.test.ts
  - api/slack/diag.ts
  - api/admin/refresh-config.ts
  - README.md
  - DEPLOY.md
autonomous: true
requirements: [SEC-04, SEC-05, SEC-06]
must_haves:
  truths:
    - "With OPS_API_TOKEN unset, /api/slack/diag and /api/admin/refresh-config return 404 (fail-closed, no ops surface)"
    - "With OPS_API_TOKEN set, both endpoints require Authorization: Bearer <token> and return 401 on a missing/wrong token (timing-safe)"
    - "refresh-config rejects GET with 405 and performs its cache clear only on POST"
    - "diag's bot self-join runs only on POST and only targets the configured SLACK_TASK_CHANNEL_ID — an attacker-supplied arbitrary channel is ignored"
    - "diag and refresh-config responses expose counts/booleans, not full channel lists, Redis host, or cache key names"
    - "The Slack signing secret is no longer used as the ops gate on either endpoint"
  artifacts:
    - path: "src/ops/auth.ts"
      provides: "Pure, testable ops-token gate: disabled (404) / unauthorized (401) / ok, with timing-safe Bearer compare"
      exports: ["evaluateOpsAuth"]
    - path: "src/ops/auth.test.ts"
      provides: "Unit coverage for disabled/unauthorized/ok states"
    - path: "api/slack/diag.ts"
      provides: "Bearer-gated diag: GET report (reduced disclosure) + POST self-join to configured channel only"
    - path: "api/admin/refresh-config.ts"
      provides: "Bearer-gated POST-only cache refresh returning a cleared count"
  key_links:
    - from: "api/slack/diag.ts"
      to: "src/ops/auth.ts"
      via: "evaluateOpsAuth(env.OPS_API_TOKEN, authHeader)"
      pattern: "evaluateOpsAuth"
    - from: "api/admin/refresh-config.ts"
      to: "src/ops/auth.ts"
      via: "evaluateOpsAuth(env.OPS_API_TOKEN, authHeader)"
      pattern: "evaluateOpsAuth"
---

<objective>
Harden the two ops endpoints (`/api/slack/diag`, `/api/admin/refresh-config`) per 08-CONTEXT FIND-01/FIND-02/FIND-03. Replace the `?secret=<SLACK_SIGNING_SECRET>` query gate with a dedicated, optional `OPS_API_TOKEN` checked via `Authorization: Bearer` (timing-safe). Fail closed: when the token is unset the endpoints return 404. Require POST for mutations, restrict diag self-join to the configured channel, and trim response disclosure. Update README + DEPLOY to the new auth model.

Purpose: Close both High findings (secret-in-URL credential overload; state-change-over-GET + arbitrary self-join) and the Medium topology-disclosure finding without touching the working bot flow.
Output: New `src/ops/auth.ts` gate, rewritten endpoints, optional env var, updated docs, passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-security-hardening/08-CONTEXT.md
@.planning/phases/07-security-audit/SECURITY.md

<interfaces>
<!-- Existing env loader — extend, do not rewrite. Optional vars must NOT fail-fast. -->
From src/config/env.ts:
  export type Env = z.infer<typeof EnvSchema>;
  export function loadEnv(source?: Record<string, string | undefined>): Env;
  // EnvSchema currently has only required (nonEmpty) and defaulted vars.

<!-- Existing safeEqual pattern duplicated in both endpoints (reuse the logic, centralize it). -->
From api/slack/diag.ts + api/admin/refresh-config.ts:
  function safeEqual(a: string, b: string): boolean; // Buffer length guard + crypto.timingSafeEqual
  // diag currently: GET, secret in ?secret=, ?join=<arbitrary>, returns full channel list + redis host
  // refresh-config currently: GET, secret in ?secret=, returns { cleared: string[] } (key names)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional OPS_API_TOKEN + the ops-auth gate</name>
  <files>src/config/env.ts, src/config/env.test.ts, src/ops/auth.ts, src/ops/auth.test.ts</files>
  <behavior>
    - evaluateOpsAuth(undefined, anyHeader) === "disabled"
    - evaluateOpsAuth("", anyHeader) === "disabled" (empty/whitespace token = not configured)
    - evaluateOpsAuth("tok", null) === "unauthorized"
    - evaluateOpsAuth("tok", "Bearer wrong") === "unauthorized"
    - evaluateOpsAuth("tok", "tok") === "unauthorized" (must carry the Bearer scheme)
    - evaluateOpsAuth("tok", "Bearer tok") === "ok"
    - Comparison is timing-safe (Buffer length guard before crypto.timingSafeEqual; never throws on mismatched lengths or malformed header)
    - loadEnv accepts a source WITHOUT OPS_API_TOKEN and still returns a valid Env (optional, no fail-fast)
    - loadEnv with OPS_API_TOKEN="" leaves env.OPS_API_TOKEN as undefined/empty so the gate treats it as disabled
  </behavior>
  <action>
    In src/config/env.ts add `OPS_API_TOKEN` to EnvSchema as OPTIONAL with NO min-length constraint (e.g. `z.string().trim().optional()`), so a missing OR empty value never trips the fail-fast Zod validation — that is the FIND-01 decision (optional, fail-closed at the gate, not at boot). Do not change any existing required/defaulted var. Add an env.test.ts case asserting loadEnv succeeds when OPS_API_TOKEN is absent.

    Create src/ops/auth.ts exporting `evaluateOpsAuth(opsToken: string | undefined, authHeader: string | null): "disabled" | "unauthorized" | "ok"`. Logic: trim opsToken; if empty/undefined → "disabled". Else parse the Bearer scheme from authHeader (case-insensitive `Bearer ` prefix; return "unauthorized" if absent), extract the token, and compare to opsToken with a timing-safe equal that length-guards before crypto.timingSafeEqual (reuse the exact safeEqual shape from the current endpoints). Return "ok" only on an exact match, else "unauthorized". This centralizes the gate so the endpoints stay thin and the logic is unit-tested without booting full env (per SEC-04, FIND-01).

    Create src/ops/auth.test.ts covering every bullet in <behavior>.
  </action>
  <verify>
    <automated>npm test -- run src/ops/auth.test.ts src/config/env.test.ts && npm run typecheck</automated>
  </verify>
  <done>OPS_API_TOKEN is an optional env var; evaluateOpsAuth returns disabled/unauthorized/ok exactly per the behavior table; new tests + full typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite diag + refresh-config endpoints on the Bearer gate</name>
  <files>api/slack/diag.ts, api/admin/refresh-config.ts</files>
  <action>
    Both endpoints: read the `Authorization` header from the request, call `evaluateOpsAuth(env.OPS_API_TOKEN, authHeader)`. On "disabled" return `new Response("not found", { status: 404 })` (fail-closed — no ops surface when unconfigured, FIND-01/FIND-02). On "unauthorized" return 401. Only on "ok" proceed. Remove the `?secret=` query reading and the local `safeEqual`/SLACK_SIGNING_SECRET usage entirely — stop overloading the signing secret (FIND-01). Never log the token.

    api/admin/refresh-config.ts: export the handler as POST only (rename `GET` → `POST`). A GET must return 405 `{ allow: "POST" }` (state change requires POST, FIND-02). Keep the existing cfg:* clear logic (drop `:lastgood`) but change the response to a count, not the key names: `{ clearedCount: toClear.length }` (FIND-03 / IN-03 reduced disclosure).

    api/slack/diag.ts: keep `GET` as the read-only report but reduce disclosure (FIND-03) — return counts/booleans only: `{ taskChannelJoined: boolean, channelCount: number, redisOk: boolean, redisUrlScheme: string, botUserId, botName, team }`. Do NOT return the full `inChannels` list, do NOT return `redisUrlHost`, do NOT return cache key names. Add a `POST` handler for the self-join action: it joins the bot ONLY to `env.SLACK_TASK_CHANNEL_ID` (ignore/deny any attacker-supplied `?join=` — remove the arbitrary `join` query param read entirely, FIND-02), then returns the same reduced report. Keep the per-Slack-call try/catch so a missing-scope error is reported, not thrown. Preserve the timing-safe gate ordering: auth FIRST, before any Slack/Redis call.

    Both handlers keep `const env = loadEnv()` at module top unchanged.
  </action>
  <verify>
    <automated>npm run typecheck && grep -Lq "SLACK_SIGNING_SECRET" api/slack/diag.ts api/admin/refresh-config.ts && grep -q "evaluateOpsAuth" api/slack/diag.ts && grep -q "evaluateOpsAuth" api/admin/refresh-config.ts && grep -q "export const POST" api/admin/refresh-config.ts && echo OK</automated>
  </verify>
  <done>Neither endpoint references SLACK_SIGNING_SECRET or `?secret=`; both gate via evaluateOpsAuth (404 disabled / 401 unauthorized / proceed); refresh-config is POST-only (405 on GET) returning clearedCount; diag self-join is POST-only and hardwired to SLACK_TASK_CHANNEL_ID; diag report is reduced to counts/booleans; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update README + DEPLOY ops instructions to OPS_API_TOKEN + Bearer</name>
  <files>README.md, DEPLOY.md</files>
  <action>
    In README.md replace the `GET /api/admin/refresh-config?secret=<SLACK_SIGNING_SECRET>` block and its "gated by the Slack signing secret … the same gate the diag endpoint uses" prose with the new model: set an optional `OPS_API_TOKEN` env var (a random 32+ char value, e.g. `openssl rand -hex 24`); when unset both ops endpoints return 404; when set, call them with `Authorization: Bearer $OPS_API_TOKEN`. Show `curl -X POST -H "Authorization: Bearer $OPS_API_TOKEN" https://<DEPLOY_URL>/api/admin/refresh-config` and the diag GET/POST equivalents. State that diag's POST self-join only joins the configured task channel.

    In DEPLOY.md add `OPS_API_TOKEN` to the env-vars list as OPTIONAL (note: leave unset to keep diag/refresh-config disabled; set it to enable ops access) and document the Bearer usage in place of any `?secret=` reference. Keep wording in the existing Spanish style of DEPLOY.md.

    Humanize the doc prose (no AI tells, no em/en dashes).
  </action>
  <verify>
    <automated>grep -q "OPS_API_TOKEN" README.md && grep -q "OPS_API_TOKEN" DEPLOY.md && ! grep -q "refresh-config?secret=" README.md && echo OK</automated>
  </verify>
  <done>Both docs describe OPS_API_TOKEN + Authorization: Bearer, note the 404-when-unset fail-closed behavior, and no longer reference the old `?secret=` ops gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ops endpoint | Untrusted HTTP hitting diag/refresh-config; previously gated by signing secret in URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing/Elevation | ops endpoints auth | mitigate | Dedicated OPS_API_TOKEN via Bearer header, timing-safe compare; signing secret no longer reused (FIND-01) |
| T-08-02 | Tampering | diag self-join, refresh-config mutation | mitigate | Mutations require POST; diag join hardwired to SLACK_TASK_CHANNEL_ID; fail-closed 404 when token unset (FIND-02) |
| T-08-03 | Info Disclosure | diag/refresh responses | mitigate | Return counts/booleans only; drop channel list, redis host, cache key names (FIND-03) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan; nothing to install |
</threat_model>

<verification>
- `npm test` full suite stays green (291 existing + new ops tests), `npm run typecheck` clean.
- Grep confirms no SLACK_SIGNING_SECRET / `?secret=` ops gate remains.
- Live re-verify (curl against the deploy with/without OPS_API_TOKEN) is human-deferred per phase policy.
</verification>

<success_criteria>
SEC-04 (gate ops endpoints), SEC-05/FIND-01 (Bearer token, no secret-in-URL), SEC-05/FIND-02 (POST + restricted self-join + fail-closed), SEC-06/FIND-03 (reduced disclosure) are all implemented; docs updated; offline tests + typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/08-security-hardening/08-01-SUMMARY.md` when done.
</output>
