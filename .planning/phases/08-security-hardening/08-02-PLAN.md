---
phase: 08-security-hardening
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/util/slackMrkdwn.ts
  - src/clickup/webhook.ts
  - src/slack/blocks.ts
  - src/slack/blocks.test.ts
  - src/clickup/client.ts
  - src/clickup/client.test.ts
  - .planning/phases/07-security-audit/SECURITY.md
autonomous: true
requirements: [SEC-05, SEC-06, SEC-07]
must_haves:
  truths:
    - "buildPreviewBlocks escapes Slack mrkdwn control chars (&, <, >) in title, description, links, and resolved/unresolved assignee + cliente text so a crafted title like <!channel> or <@U123> cannot ping/spoof in the preview"
    - "getTask rejects a taskId that is not ^[A-Za-z0-9]+$ before any fetch (no malformed segment reaches the ClickUp URL path)"
    - "The ClickUp webhook replay residual (no signed timestamp) is documented in-code where verification happens, keeping the 24h dedup as the accepted bound"
    - "SECURITY.md marks every addressed finding fixed with file:line and every accepted finding with rationale"
    - "SECURITY.md records the SEC-07 dependency posture: npm audit --omit=dev is 0, dev-only advisories accepted"
  artifacts:
    - path: "src/util/slackMrkdwn.ts"
      provides: "Shared escapeSlackText helper (moved from clickup/webhook.ts), importable by both clickup and slack layers"
      exports: ["escapeSlackText"]
    - path: "src/slack/blocks.ts"
      provides: "Preview builder that escapes all untrusted interpolated fields"
    - path: "src/clickup/client.ts"
      provides: "getTask with taskId format validation before fetch"
  key_links:
    - from: "src/slack/blocks.ts"
      to: "src/util/slackMrkdwn.ts"
      via: "import escapeSlackText"
      pattern: "escapeSlackText"
    - from: "src/clickup/webhook.ts"
      to: "src/util/slackMrkdwn.ts"
      via: "re-export / import escapeSlackText"
      pattern: "escapeSlackText"
---

<objective>
Close the remaining input-safety findings and formally close the audit. Escape outbound Slack mrkdwn in the preview (FIND-07), validate the ClickUp taskId path segment (FIND-11), document the webhook replay residual (FIND-04), re-confirm no secret leaks (SEC-06), record the dependency posture (SEC-07 / FIND-05), and update SECURITY.md status for all findings — including the FIND-01/02/03 fixes shipped in 08-01.

Purpose: Finish the surgical hardening pass and leave SECURITY.md as an accurate closed-status record.
Output: Shared escape helper, escaped preview, validated taskId, replay-residual comment, updated SECURITY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-security-hardening/08-CONTEXT.md
@.planning/phases/07-security-audit/SECURITY.md

<interfaces>
<!-- escapeSlackText currently lives in clickup/webhook.ts and is its only definer/user. -->
From src/clickup/webhook.ts:
  export function escapeSlackText(text: string): string; // &→&amp; <→&lt; >→&gt; (order: & first)

<!-- Preview builder interpolates these UNTRUSTED fields raw into mrkdwn today (blocks.ts:79-87): -->
From src/slack/blocks.ts:
  export function buildPreviewBlocks(pendingId, resolved: ResolvedTask, opts): Block[];
  // raw fields: resolved.title, resolved.description, resolved.links[], clienteLine(), asignadosLine() (incl. unresolvedAssignees)

<!-- getTask interpolates taskId straight into the URL path (client.ts:151-152). -->
From src/clickup/client.ts:
  getTask(taskId: string): Promise<GetTaskResult>; // fetch(`${BASE_URL}/task/${taskId}`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share escapeSlackText + escape the preview fields</name>
  <files>src/util/slackMrkdwn.ts, src/clickup/webhook.ts, src/slack/blocks.ts, src/slack/blocks.test.ts</files>
  <behavior>
    - buildPreviewBlocks with a title `<!channel>` renders `&lt;!channel&gt;` (no live ping) in the summary section text
    - A description containing `<@U123>` and `&` renders escaped (`&lt;@U123&gt;`, `&amp;`)
    - A link containing `<` / `>` is escaped
    - An unresolved assignee string like `<!here>` renders escaped (the ⚠️ prefix is preserved, the untrusted text is escaped)
    - The static Spanish labels (*Título:*, *Cliente:* etc.) and the ⚠️ warning markers remain intact (only interpolated untrusted values are escaped)
    - Existing blocks.test.ts cases still pass (confirm/cancel terminal blocks unchanged)
  </behavior>
  <action>
    Create src/util/slackMrkdwn.ts and MOVE the `escapeSlackText` function there verbatim (it has no deps). In src/clickup/webhook.ts, delete the local definition and `import { escapeSlackText } from "../util/slackMrkdwn.js"` AND re-export it (`export { escapeSlackText } ...`) so the existing `webhook.ts` export surface and its tests keep working unchanged. This avoids a slack→clickup import coupling (FIND-07 / SEC-06 decision: "move/share the helper").

    In src/slack/blocks.ts import `escapeSlackText` from the shared util and apply it to every UNTRUSTED interpolated value rendered into mrkdwn: `resolved.title`, the description string, each `resolved.links` entry, the resolved cliente name in `clienteLine`, and the member/unresolved-assignee names in `asignadosLine`. Do NOT escape the static Spanish labels, the ⚠️ markers, or the formatted dates (trusted). Keep the placeholder fallbacks (`_(sin descripción)_`, `_(sin links)_`) as-is — those are trusted literals.

    Add the <behavior> cases to src/slack/blocks.test.ts.
  </action>
  <verify>
    <automated>npm test -- run src/slack/blocks.test.ts src/clickup/webhook.test.ts && npm run typecheck</automated>
  </verify>
  <done>escapeSlackText lives in src/util/slackMrkdwn.ts, re-exported from webhook.ts; buildPreviewBlocks escapes all untrusted fields; new + existing tests and typecheck pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate taskId in getTask + document webhook replay residual</name>
  <files>src/clickup/client.ts, src/clickup/client.test.ts, src/clickup/webhook.ts</files>
  <behavior>
    - getTask("abc123") proceeds to fetch as today (valid id, happy path unchanged)
    - getTask("../../team/9013/member") throws BEFORE any fetch call (the injected fetch mock is never invoked)
    - getTask("") and getTask("has space") and getTask("id/slash") throw before fetch
    - The thrown error message does not include the Authorization token
  </behavior>
  <action>
    In src/clickup/client.ts getTask, before the fetch, validate `taskId` against `^[A-Za-z0-9]+$`; if it fails, throw `new Error("ClickUp getTask: invalid taskId format")` (defense-in-depth at the trust boundary, FIND-11 — ClickUp ids are alphanumeric). Do not change the happy path or the response parsing.

    Add src/clickup/client.test.ts cases per <behavior> using an injected fetch spy that asserts it is NOT called for the malformed ids.

    In src/clickup/webhook.ts, add a concise code comment at the dedup/delivery-key path (buildDeliveryKey / runWebhook step 4) documenting the FIND-04 residual: the ClickUp payload carries no signed timestamp, so timestamp-window replay rejection is not possible without inventing a field; the 24h `whk:` dedup TTL is the accepted replay bound (per 08-CONTEXT FIND-04). Do NOT add a fake timestamp check — document only.
  </action>
  <verify>
    <automated>npm test -- run src/clickup/client.test.ts && npm run typecheck && grep -q "FIND-04" src/clickup/webhook.ts && echo OK</automated>
  </verify>
  <done>getTask rejects non-alphanumeric taskIds before fetch (proven by an uncalled fetch spy); the replay residual is documented in webhook.ts; tests + typecheck pass.</done>
</task>

<task type="auto">
  <name>Task 3: Close the audit — update SECURITY.md status + dependency posture</name>
  <files>.planning/phases/07-security-audit/SECURITY.md</files>
  <action>
    Update SECURITY.md to reflect Phase 8 closure. For each finding add a status:
    - FIND-01 → FIXED: OPS_API_TOKEN Bearer gate replaces signing-secret-in-URL (api/slack/diag.ts, api/admin/refresh-config.ts; src/ops/auth.ts evaluateOpsAuth).
    - FIND-02 → FIXED: mutations require POST; diag self-join hardwired to SLACK_TASK_CHANNEL_ID; fail-closed 404 when token unset (api/slack/diag.ts, api/admin/refresh-config.ts).
    - FIND-03 → FIXED: diag/refresh responses reduced to counts/booleans (api/slack/diag.ts, api/admin/refresh-config.ts).
    - FIND-04 → ACCEPTED (documented): no signed ClickUp timestamp; 24h whk: dedup is the replay bound (src/clickup/webhook.ts).
    - FIND-05 → ACCEPTED: npm audit --omit=dev is 0 runtime vulns; the 21 advisories are dev-only (vercel CLI, vitest/vite/esbuild) and never ship to runtime (SEC-07). Note that a toolchain major bump is optional hygiene, not required for runtime safety.
    - FIND-06 → ACCEPTED for v1.1: rate limiting deferred (signature gates + Vercel platform are the practical limiter).
    - FIND-07 → FIXED: outbound preview now escapes mrkdwn via shared escapeSlackText (src/slack/blocks.ts, src/util/slackMrkdwn.ts).
    - FIND-08 → ACCEPTED: kill-switch fail-open is an intentional availability decision (HARD-03).
    - FIND-09 → ACCEPTED/noted: 64-bit content-hash dedup key collision risk negligible at this volume (left as-is unless trivially widened).
    - FIND-10 → ACCEPTED for v1.1: ops audit-trail/alerting deferred.
    - FIND-11 → FIXED: getTask validates taskId ^[A-Za-z0-9]+$ before fetch (src/clickup/client.ts).
    - FIND-12 / FIND-13 → note disposition (optional headers / repo hygiene confirmed).

    Re-confirm SEC-06: state that the new ops responses and error bodies expose no token/secret/email (grep-verified clean after the changes). Use exact file:line references where practical. Keep the existing audit body; append/adjust a "## Phase 8 Closure Status" section or annotate the remediation table — your discretion. Humanize prose (no AI tells, no em/en dashes).
  </action>
  <verify>
    <automated>grep -q "FIXED" .planning/phases/07-security-audit/SECURITY.md && grep -q "omit=dev" .planning/phases/07-security-audit/SECURITY.md && grep -q "FIND-11" .planning/phases/07-security-audit/SECURITY.md && echo OK</automated>
  </verify>
  <done>SECURITY.md records FIXED-with-location for FIND-01/02/03/07/11 and ACCEPTED-with-rationale for FIND-04/05/06/08/09/10; SEC-07 dependency posture (0 runtime, dev-only accepted) and SEC-06 no-leak re-confirmation are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/user text → Slack preview | Untrusted task title/description/links/names interpolated into mrkdwn |
| ClickUp webhook taskId → fetch path | Webhook-supplied id interpolated into the ClickUp URL path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing | outbound preview mrkdwn | mitigate | escapeSlackText on all untrusted interpolated preview fields (FIND-07) |
| T-08-05 | Tampering | getTask URL path | mitigate | Validate taskId ^[A-Za-z0-9]+$ before fetch (FIND-11) |
| T-08-06 | Repudiation/Replay | ClickUp webhook | accept | No signed timestamp available; 24h dedup is the bound, documented (FIND-04) |
| T-08-07 | Vulnerable components | npm deps | accept | npm audit --omit=dev = 0 runtime; dev-only advisories accepted (SEC-07/FIND-05) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan; nothing to install |
</threat_model>

<verification>
- `npm test` full suite green (existing 291 + new), `npm run typecheck` clean.
- Grep confirms preview escaping, taskId validation, FIND-04 comment, and SECURITY.md status updates.
- Live re-verify is human-deferred per phase policy.
</verification>

<success_criteria>
SEC-05/FIND-07 (preview escape), SEC-05/FIND-11 (taskId validation), FIND-04 residual documented, SEC-06 no-leak re-confirmed, SEC-07/FIND-05 dependency posture recorded, and SECURITY.md closed-status accurate for all findings.
</success_criteria>

<output>
Create `.planning/phases/08-security-hardening/08-02-SUMMARY.md` when done.
</output>
