---
phase: 01-serverless-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/config/env.ts
  - src/config/env.test.ts
  - src/store/redis.ts
  - src/store/redis.test.ts
autonomous: true
requirements: [INGEST-03]

must_haves:
  truths:
    - "A missing or malformed env var fails fast at startup with a clear, specific error"
    - "Marking an event_id for the first time returns true; the same event_id within TTL returns false"
    - "Dedup uses SET ... NX EX so the value persists in Redis across cold starts and is readable on the next invocation"
  artifacts:
    - path: "src/config/env.ts"
      provides: "zod-validated, typed env accessor with fail-fast"
      contains: "z.object"
    - path: "src/store/redis.ts"
      provides: "Upstash REST client factory + markEventOnce dedup helper"
      contains: "markEventOnce"
  key_links:
    - from: "src/store/redis.ts"
      to: "Upstash REST"
      via: "UPSTASH_REDIS_REST_URL / UPSTASH_REDIS_REST_TOKEN from env"
      pattern: "UPSTASH_REDIS_REST_URL"
    - from: "src/store/redis.ts markEventOnce"
      to: "Redis SET NX EX"
      via: "set(key, 1, { nx: true, ex: ttl })"
      pattern: "nx"
---

<objective>
Provide the two stateless-serverless primitives every later wave depends on: fail-fast env validation (so a misconfigured deploy dies loudly, not silently) and an Upstash Redis dedup helper keyed on Slack `event_id` (the real guard against Slack's retry-on-slow-ack duplicate processing — Pitfall 1).

Purpose: Centralize config and idempotency so the Slack ingress (plan 03) only wires them together.
Output: src/config/env.ts (+ test), src/store/redis.ts (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-serverless-foundation/01-CONTEXT.md
@.planning/phases/01-serverless-foundation/SKELETON.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts later plans (03) consume directly — no exploration needed. -->
src/config/env.ts exports:
  - type Env = { SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_TASK_CHANNEL_ID, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, TEAM_TIMEZONE }
  - function loadEnv(source?: Record<string,string|undefined>): Env   // throws on invalid, default source = process.env

src/store/redis.ts exports:
  - function createRedis(env?: Pick<Env,'UPSTASH_REDIS_REST_URL'|'UPSTASH_REDIS_REST_TOKEN'>): Redis
  - function markEventOnce(redis: RedisLike, eventId: string, ttlSeconds?: number): Promise<boolean>
    // returns true if this is the first time (key set), false if already present (retry/duplicate)
  - type RedisLike = { set(key: string, value: unknown, opts: { nx: true; ex: number }): Promise<string|null> }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Env validation with zod (fail-fast)</name>
  <files>src/config/env.ts, src/config/env.test.ts</files>
  <behavior>
    - Test: loadEnv with all required vars present returns a fully typed Env object.
    - Test: loadEnv with a missing required var (e.g. no SLACK_SIGNING_SECRET) throws an Error whose message names the missing key.
    - Test: loadEnv with an empty-string required var throws (treated as missing).
    - Test: TEAM_TIMEZONE defaults to "America/Caracas" when absent; required vars have NO defaults.
  </behavior>
  <action>
    Write the tests first (RED). Implement src/config/env.ts with a zod schema: SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_TASK_CHANNEL_ID, UPSTASH_REDIS_REST_URL (url), UPSTASH_REDIS_REST_TOKEN as non-empty strings; TEAM_TIMEZONE optional defaulting to "America/Caracas". loadEnv parses the source (default process.env) and on failure throws an Error that lists the offending keys (use zod's error to build a readable message). Export the inferred Env type. Do not read process.env at module top level — read it inside loadEnv so tests can inject a source and so import never crashes.
  </action>
  <verify>
    <automated>npx vitest run src/config/env.test.ts</automated>
  </verify>
  <done>All env tests green; missing/empty required var throws a key-naming error; TEAM_TIMEZONE default applied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upstash Redis client + event dedup helper</name>
  <files>src/store/redis.ts, src/store/redis.test.ts</files>
  <behavior>
    - Test: markEventOnce returns true when the fake Redis set(...) resolves "OK" (key was new).
    - Test: markEventOnce returns false when set(...) resolves null (key already existed → Slack retry).
    - Test: markEventOnce calls set with { nx: true, ex: <ttl> } and a namespaced key like "evt:<eventId>"; default ttl is 600 seconds (10 min) per CONTEXT.
    - Test: createRedis throws/surfaces clearly if URL or token is absent.
  </behavior>
  <action>
    Write tests first (RED), injecting a fake RedisLike so no live Upstash is needed. Implement src/store/redis.ts: createRedis builds an @upstash/redis REST client from env (UPSTASH_REDIS_REST_URL/TOKEN). markEventOnce(redis, eventId, ttlSeconds = 600) calls redis.set(`evt:${eventId}`, 1, { nx: true, ex: ttlSeconds }) and returns true when the result is truthy ("OK"), false when null (key already present → it is a retry, per dedup decision in CONTEXT and Pitfall 1). Keep markEventOnce dependency-injected (accept a RedisLike) so it is unit-testable and reusable by plan 03.
  </action>
  <verify>
    <automated>npx vitest run src/store/redis.test.ts</automated>
  </verify>
  <done>markEventOnce returns true-then-false on duplicate event_id, uses SET NX EX with 600s TTL and an evt: prefix, all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| function → Upstash REST | Network call carrying the Redis REST token |
| env → runtime | Misconfiguration must fail loudly, not produce undefined behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information disclosure | Redis REST token | mitigate | Token read only from env via loadEnv; never logged; client built lazily |
| T-01-03 | Denial of service / replay | duplicate event processing | mitigate | markEventOnce SET NX EX dedups Slack retries on event_id (the idempotency guard for INGEST-03) |
| T-01-06 | Tampering | malformed/missing config | mitigate | zod fail-fast rejects bad env at startup with named keys |
</threat_model>

<verification>
- `npx vitest run src/config/env.test.ts src/store/redis.test.ts` all green.
- markEventOnce demonstrably returns true once then false for the same event_id.
</verification>

<success_criteria>
Env validation fails fast with clear errors; Redis dedup helper idempotent on event_id via SET NX EX, both fully unit-tested without live services.
</success_criteria>

<output>
Create `.planning/phases/01-serverless-foundation/01-02-SUMMARY.md` when done.
</output>
