---
phase: 01-serverless-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vercel.json
  - .gitignore
  - .env.example
  - README.md
  - vitest.config.ts
autonomous: false
requirements: [INGEST-03]
user_setup:
  - service: vercel
    why: "Fluid Compute must be enabled for waitUntil background work (verified at the deploy checkpoint in plan 03)"
    dashboard_config:
      - task: "Enable Fluid Compute"
        location: "Vercel Project → Settings → Functions → Fluid Compute"
  - service: upstash
    why: "Serverless Redis store for event dedup and later-phase state"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Vercel Marketplace → Upstash Redis integration (or Upstash console → REST API)"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Vercel Marketplace → Upstash Redis integration (or Upstash console → REST API)"

must_haves:
  truths:
    - "Project compiles under TypeScript strict mode with zero errors"
    - "npm test runs Vitest (no tests yet is acceptable — runner is wired)"
    - "Only the locked dependency set is installed; .env is git-ignored"
  artifacts:
    - path: "package.json"
      provides: "ESM project, locked deps, test/build scripts"
      contains: "@vercel/slack-bolt"
    - path: "tsconfig.json"
      provides: "strict TypeScript config"
      contains: "\"strict\": true"
    - path: "vercel.json"
      provides: "Node 20 runtime + maxDuration for Fluid Compute waitUntil"
      contains: "maxDuration"
    - path: ".env.example"
      provides: "documented env contract"
      contains: "SLACK_SIGNING_SECRET"
    - path: ".gitignore"
      provides: "secret + build hygiene"
      contains: ".env"
  key_links:
    - from: "package.json scripts.test"
      to: "vitest"
      via: "npm test"
      pattern: "vitest"
    - from: "tsconfig.json"
      to: "strict mode"
      via: "compilerOptions.strict"
      pattern: "\"strict\""
---

<objective>
Establish the greenfield repo skeleton: an ESM TypeScript project with strict compilation, the locked dependency set, Vercel Fluid-Compute function config, secret hygiene, a documented env contract, and a wired Vitest runner. This is the Walking Skeleton's scaffold — every later task builds on it.

Purpose: Give executors a buildable, testable project so subsequent waves only add domain code, never re-litigate stack or config.
Output: package.json, tsconfig.json, vercel.json, .gitignore, .env.example, README.md, vitest.config.ts, installed node_modules + lockfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-serverless-foundation/01-CONTEXT.md
@.planning/phases/01-serverless-foundation/SKELETON.md
@.planning/research/STACK.md

<dependency_audit>
<!-- Fallback policy per Package Legitimacy Gate: RESEARCH has no "## Package Legitimacy Audit" table, so all packages are treated as [ASSUMED] and gated by a blocking-human checkpoint (Task 1) before install. All are first-party official packages with versions verified live against npm in STACK.md. -->

Runtime deps (locked in CONTEXT.md):
- @slack/bolt@^4 (verified 4.7.3) — official Slack framework — [ASSUMED]
- @vercel/slack-bolt@^1.5 (verified 1.5.0) — official Vercel adapter — [ASSUMED]
- @vercel/functions@^3 (verified 3.7.1) — official Vercel waitUntil — [ASSUMED]
- @upstash/redis@latest — official Upstash REST client — [ASSUMED]
- zod@^4 (verified 4.4.3) — [ASSUMED]

Dev deps:
- typescript@^5.6, @types/node@^20 (Node 20 per CONTEXT — research suggested 22), vitest@latest, vercel CLI — [ASSUMED]

Do NOT install: Vercel KV (sunset), openai / @anthropic-ai/sdk (Phase 2, no LLM in Phase 1).
</dependency_audit>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify dependency legitimacy before install</name>
  <what-built>Nothing yet — this gate runs BEFORE any npm install. The plan installs six runtime/dev packages treated as [ASSUMED] under the Package Legitimacy Gate fallback (RESEARCH has no audit table).</what-built>
  <how-to-verify>
    Confirm each package is the genuine, official package on npmjs.com (correct publisher, recent downloads, not a typosquat):
    1. https://www.npmjs.com/package/@slack/bolt — publisher: Slack
    2. https://www.npmjs.com/package/@vercel/slack-bolt — publisher: Vercel
    3. https://www.npmjs.com/package/@vercel/functions — publisher: Vercel
    4. https://www.npmjs.com/package/@upstash/redis — publisher: Upstash
    5. https://www.npmjs.com/package/zod — publisher: colinhacks
    These match the live-npm-verified versions in STACK.md.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold project, install deps, write config</name>
  <files>package.json, tsconfig.json, vercel.json, .gitignore, .env.example, README.md, vitest.config.ts</files>
  <action>
    Create an ESM TypeScript project named for the Slack→ClickUp bot.

    package.json: set "type": "module"; "engines": { "node": ">=20 <21" }; scripts: "build" → "tsc --noEmit", "test" → "vitest run", "test:watch" → "vitest", "typecheck" → "tsc --noEmit". After writing, install the locked deps approved in Task 1: runtime @slack/bolt@^4, @vercel/slack-bolt@^1.5, @vercel/functions@^3, @upstash/redis, zod@^4; dev typescript@^5.6, @types/node@^20, vitest, vercel. Do NOT install Vercel KV, openai, or @anthropic-ai/sdk. Generate the lockfile.

    tsconfig.json: compilerOptions with "strict": true, "target": "ES2022", "module": "NodeNext", "moduleResolution": "NodeNext", "esModuleInterop": true, "skipLibCheck": true, "noUncheckedIndexedAccess": true, "forceConsistentCasingInFileNames": true, "resolveJsonModule": true, "types": ["node"]; include ["src", "api", "**/*.test.ts"].

    vercel.json: configure the Slack function for the Node 20 runtime and a maxDuration high enough for background waitUntil work (set "functions": { "api/**/*.ts": { "runtime": "nodejs20.x", "maxDuration": 60 } }). Do NOT attempt to toggle Fluid Compute here — it is a project-setting (dashboard) captured in user_setup and verified at the deploy checkpoint (per CONTEXT, research note that waitUntil requires Fluid Compute enabled).

    .gitignore: node_modules, .env, .env.local, .vercel, dist, coverage.

    .env.example: document every var from CONTEXT.md with placeholder values and a one-line comment each — SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_TASK_CHANNEL_ID, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, and TEAM_TIMEZONE (default America/Caracas, seeded for later phases).

    vitest.config.ts: Node environment, include "**/*.test.ts".

    README.md: project one-liner plus a "## Environment" section listing the same vars as .env.example with where each comes from, plus a "## Vercel setup" note to enable Fluid Compute and provision Upstash Redis via the Vercel Marketplace.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | grep -qiv "error" && grep -q '"strict": true' tsconfig.json && grep -q "@vercel/slack-bolt" package.json && grep -q "SLACK_SIGNING_SECRET" .env.example && grep -qx ".env" .gitignore</automated>
  </verify>
  <done>tsc --noEmit passes (no source yet), npm test runs the Vitest runner clean, strict mode on, locked deps present (no KV/openai), .env ignored, README documents env + Fluid Compute + Upstash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build | Installed dependencies execute install scripts and run in production |
| repo → git remote | Secrets must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs | mitigate | Task 1 blocking-human checkpoint verifies each [ASSUMED] package on npmjs.com before install; install only the locked set |
| T-01-04 | Information disclosure | .env / secrets | mitigate | .gitignore ignores .env*; secrets documented in .env.example with placeholders only, never real values |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test` runs Vitest without errors.
- No `kv`, `openai`, or `@anthropic-ai/sdk` entries in package.json.
</verification>

<success_criteria>
Strict-mode ESM TypeScript project builds, Vitest is wired, locked deps installed, secret hygiene and env contract documented.
</success_criteria>

<output>
Create `.planning/phases/01-serverless-foundation/01-01-SUMMARY.md` when done.
</output>
