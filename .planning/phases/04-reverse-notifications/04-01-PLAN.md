---
phase: 04-reverse-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/env.ts
  - src/config/env.test.ts
  - src/clickup/signature.ts
  - src/clickup/signature.test.ts
  - src/store/redis.ts
  - src/store/redis.test.ts
autonomous: true
requirements: [NOTIFY-01]
must_haves:
  truths:
    - "A request whose X-Signature equals HMAC-SHA256(rawBody, secret) is accepted"
    - "A request with a tampered body, wrong secret, or missing header is rejected"
    - "The X-Signature header is found regardless of header-name casing"
    - "A redelivered ClickUp webhook (same delivery key) is recognized as a duplicate"
    - "CLICKUP_WEBHOOK_SECRET is required and a misconfigured deploy fails fast"
  artifacts:
    - path: "src/clickup/signature.ts"
      provides: "Raw-body X-Signature HMAC-SHA256 verifier + case-insensitive header lookup"
      exports: ["verifyClickUpSignature", "getClickUpSignatureHeader"]
    - path: "src/store/redis.ts"
      provides: "Webhook-redelivery dedup helper (SET NX EX)"
      contains: "markWebhookDeliveryOnce"
    - path: "src/config/env.ts"
      provides: "CLICKUP_WEBHOOK_SECRET (required) + CLICKUP_TEAM_ID (default)"
      contains: "CLICKUP_WEBHOOK_SECRET"
  key_links:
    - from: "src/clickup/signature.test.ts"
      to: "src/clickup/signature.ts"
      via: "self-computed HMAC proves verify logic offline"
      pattern: "createHmac"
---

<objective>
Build the offline-provable security + idempotency primitives for the ClickUp → Slack
webhook path: a raw-body `X-Signature` HMAC-SHA256 verifier (case-insensitive header
lookup, timing-safe compare), a webhook-redelivery dedup helper, and the new env vars.

Purpose: NOTIFY-01 requires the endpoint to verify ClickUp's signature over the raw body
and reject invalid requests. The exact `X-Signature` format is a flagged research gap
(CONTEXT > Claude's Discretion), so the verifier is implemented defensively and proven
against a self-computed HMAC — the logic is correct offline before the live secret exists.
Output: `src/clickup/signature.ts`, dedup helper in `src/store/redis.ts`, env additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-reverse-notifications/04-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Existing dedup primitive to mirror (src/store/redis.ts) -->
```typescript
export type RedisLike = {
  set(key: string, value: unknown, opts?: { nx?: true; ex?: number }): Promise<unknown>;
  get(key: string): Promise<unknown>;
  getdel(key: string): Promise<unknown>;
  del(...keys: string[]): Promise<unknown>;
};
// Existing pattern — copy its shape for the webhook variant:
export const DEFAULT_EVENT_TTL_SECONDS = 600;
export async function markEventOnce(redis, eventId, ttl): Promise<boolean> {
  const result = await redis.set(`evt:${eventId}`, 1, { nx: true, ex: ttl });
  return result !== null;
}
```

<!-- Existing env contract (src/config/env.ts) — zod schema, loadEnv, nonEmpty helper -->
```typescript
const nonEmpty = z.string().trim().min(1);
const EnvSchema = z.object({ /* ...existing... */
  CLICKUP_API_TOKEN: nonEmpty,
  CLICKUP_LIST_ID: nonEmpty.default("901327239630"),
});
export function loadEnv(source = process.env): Env { /* safeParse + named errors */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLICKUP_WEBHOOK_SECRET + CLICKUP_TEAM_ID to the env contract</name>
  <files>src/config/env.ts, src/config/env.test.ts</files>
  <behavior>
    - loadEnv throws (naming CLICKUP_WEBHOOK_SECRET) when the secret is absent or empty.
    - loadEnv succeeds with CLICKUP_WEBHOOK_SECRET present; CLICKUP_TEAM_ID defaults to "90131720021" when omitted and is overridable.
    - Existing required vars still validate (no regression).
  </behavior>
  <action>
    Extend EnvSchema with `CLICKUP_WEBHOOK_SECRET: nonEmpty` (required — a misconfigured
    deploy must fail fast, per CONTEXT > Endpoint) and `CLICKUP_TEAM_ID: nonEmpty.default("90131720021")`
    (used by the registration helper in plan 04-03; team/workspace id from CONTEXT > Webhook registration).
    Reuse the existing `nonEmpty` helper and the named-issue error formatting — do not change loadEnv's
    signature or the fixture-injection pattern. Add fixture entries in env.test.ts so the existing
    happy-path fixture stays valid, plus a missing-secret rejection case.
  </action>
  <verify>
    <automated>npx vitest run src/config/env.test.ts</automated>
  </verify>
  <done>CLICKUP_WEBHOOK_SECRET required + CLICKUP_TEAM_ID defaulted; env tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raw-body X-Signature verifier proven against a self-computed HMAC</name>
  <files>src/clickup/signature.ts, src/clickup/signature.test.ts</files>
  <behavior>
    - verifyClickUpSignature(rawBody, sig, secret) returns true when sig === HMAC-SHA256(rawBody, secret) as lowercase hex (sig computed by the test with node:crypto, proving the logic offline).
    - Returns false for a tampered body, wrong secret, empty/missing sig, or wrong-length sig — never throws.
    - Comparison is timing-safe (crypto.timingSafeEqual on equal-length buffers; length-guard first).
    - Defensive: tolerates an optional "sha256=" prefix on the incoming signature (format is a research gap).
    - getClickUpSignatureHeader finds the value for header names "X-Signature", "x-signature", "X-SIGNATURE" (case-insensitive) from a Headers object and from a plain record; returns null when absent.
  </behavior>
  <action>
    Create the verifier with node:crypto only (no new deps). `verifyClickUpSignature(rawBody: string,
    signature: string, secret: string): boolean` computes `createHmac("sha256", secret).update(rawBody, "utf8").digest("hex")`,
    strips an optional leading "sha256=" from the incoming signature, lowercases both, guards equal length,
    then `timingSafeEqual`. Per Pitfall 2/7: the caller (plan 04-03 ingress) passes the EXACT raw body bytes —
    document that this function must never be given a re-serialized JSON string. `getClickUpSignatureHeader`
    accepts `Headers | Record<string, string | undefined>` and does case-insensitive lookup (CONTEXT > Endpoint:
    case-insensitive header, NOTIFY-01). The test computes the expected HMAC itself so correctness is proven
    before the live ClickUp secret is known.
  </action>
  <verify>
    <automated>npx vitest run src/clickup/signature.test.ts</automated>
  </verify>
  <done>Valid self-signed payload passes; tampered/missing/wrong-secret rejected; header found case-insensitively; NOTIFY-01 crypto logic proven offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Webhook-redelivery dedup helper (SET NX EX)</name>
  <files>src/store/redis.ts, src/store/redis.test.ts</files>
  <behavior>
    - markWebhookDeliveryOnce(redis, key) returns true the first time a delivery key is seen and false on a redelivery (mirrors markEventOnce's NX semantics).
    - Uses a distinct "whk:" key namespace so it never collides with the Slack "evt:" dedup keys.
    - Honors a TTL (default DEFAULT_WEBHOOK_TTL_SECONDS) covering ClickUp's redelivery window.
  </behavior>
  <action>
    Add `DEFAULT_WEBHOOK_TTL_SECONDS = 86400` (24h redelivery window) and
    `markWebhookDeliveryOnce(redis: RedisLike, deliveryKey: string, ttl = DEFAULT_WEBHOOK_TTL_SECONDS): Promise<boolean>`
    that does `redis.set("whk:" + deliveryKey, 1, { nx: true, ex: ttl })` and returns `result !== null` —
    the same SET-NX-EX idempotency pattern as markEventOnce (CONTEXT > Dedup; Pitfall 7). Do NOT touch the
    existing task2thread / pending helpers. Add a redis.test.ts case using the existing in-memory RedisLike
    mock: first call true, second call (same key) false.
  </action>
  <verify>
    <automated>npx vitest run src/store/redis.test.ts</automated>
  </verify>
  <done>Same delivery key marked once; "whk:" namespace isolated from "evt:"; redis tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ClickUp → /api/clickup/webhook | Untrusted public HTTP; only an HMAC over the raw body proves authenticity |
| function → env vars | Webhook secret must come from Vercel env, never code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | inbound webhook | mitigate | verifyClickUpSignature: HMAC-SHA256 over raw body, timing-safe compare (this plan; enforced at ingress in 04-03) |
| T-04-02 | Tampering | re-delivered/forged payload | mitigate | markWebhookDeliveryOnce SET-NX-EX dedup + signature reject |
| T-04-03 | Information Disclosure | webhook secret | mitigate | CLICKUP_WEBHOOK_SECRET via env (zod-required); never logged |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new external packages — node:crypto is a builtin; zod/@upstash/redis already locked |
</threat_model>

<verification>
- `npm run typecheck` passes.
- `npm test` green (full suite, no regressions in env/redis/clickup).
- A self-computed HMAC verifies true; tampered/missing rejected.
</verification>

<success_criteria>
- verifyClickUpSignature + getClickUpSignatureHeader exist and pass offline self-signed tests (NOTIFY-01 crypto proven).
- markWebhookDeliveryOnce gives once-only semantics on a "whk:" namespace.
- CLICKUP_WEBHOOK_SECRET required; CLICKUP_TEAM_ID defaulted.
</success_criteria>

<output>
Create `.planning/phases/04-reverse-notifications/04-01-SUMMARY.md` when done.
</output>
