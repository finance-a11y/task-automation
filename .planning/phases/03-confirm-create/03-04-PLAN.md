---
phase: 03-confirm-create
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/slack/modal.ts
  - src/slack/modal.test.ts
  - src/slack/interactions.ts
  - src/slack/interactions.test.ts
  - src/slack/app.ts
autonomous: true
requirements: [CONFIRM-04]
must_haves:
  truths:
    - "Editar opens a Slack modal prefilled from the pending task with selects for Cliente, Asignados, and Inicio/Entrega date pickers"
    - "Submitting the modal corrects the pending in Redis and re-renders the preview message with the updated values"
    - "The modal opens within Slack's 3s trigger window"
  artifacts:
    - path: "src/slack/modal.ts"
      provides: "buildEditModal + parseEditSubmission"
      min_lines: 60
  key_links:
    - from: "src/slack/modal.ts"
      to: "src/config/clients.ts + src/config/members.ts"
      via: "static_select options from config"
      pattern: "CLIENTS|MEMBERS"
    - from: "src/slack/app.ts"
      to: "app.action edit_task + app.view edit_modal_submit"
      via: "Bolt modal lifecycle"
      pattern: "app.view"
---

<objective>
Add the Editar branch: open a prefilled Slack modal (Cliente static_select, Asignados multi_static_select, Título/Descripción inputs, Inicio/Entrega date pickers), and on submit correct the pending task in Redis and re-render the threaded preview.

Purpose: CONFIRM-04 — let the human fix any resolver miss (⚠️ fields) before confirming.
Output: new `src/slack/modal.ts`, extended `src/slack/interactions.ts` and `src/slack/app.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-confirm-create/03-CONTEXT.md

@src/slack/interactions.ts
@src/slack/app.ts
@src/config/clients.ts
@src/config/members.ts
@src/resolve/types.ts

<interfaces>
Depends on plan 03-02 (blocks, pending helpers) and 03-03 (interactions wiring):
```typescript
// src/store/redis.ts
getPending(redis, pendingId): Promise<PendingTask | null>
putPending(redis, pendingId, pending, ttl?): Promise<void>
// src/slack/blocks.ts
buildPreviewBlocks(pendingId, resolved, {timezone}): Block[]
// CLIENTS: name → optionUUID ; MEMBERS: name → numeric id
```
Slack modal facts: views.open needs body.trigger_id and must be called promptly (trigger_id expires ~3s after the click). callback_id ties the submission back; private_metadata carries JSON (pendingId, channel, messageTs) across the open→submit gap. static_select option `value` can be any string — use the option UUID for Cliente and the stringified member id for Asignados so parsing is direct. datepicker returns "YYYY-MM-DD".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Edit modal builder + submission parser</name>
  <files>src/slack/modal.ts, src/slack/modal.test.ts</files>
  <behavior>
    - buildEditModal(resolved, {pendingId, channel, messageTs, timezone}) returns a Slack view: callback_id "edit_modal_submit", private_metadata = JSON.stringify({pendingId, channel, messageTs}), and input blocks — Título (plain_text_input, initial=title), Descripción (multiline plain_text_input, optional, initial=description), Cliente (static_select over the 7 CLIENTS as {text:name, value:optionUUID}, initial_option = current clienteOptionId when set), Asignados (multi_static_select over the 9 MEMBERS as {text:name, value:String(id)}, initial_options = current assigneeIds), Inicio (datepicker, initial_date from startDateMs in timezone when set), Entrega (datepicker, initial_date from dueDateMs).
    - parseEditSubmission(view): reads the view state values + private_metadata and returns { meta:{pendingId,channel,messageTs}, patch: Partial<ResolvedTask> } where patch carries the corrected title, description (null when blank), clienteOptionId (selected value or null), assigneeIds (selected values parsed to numbers), startDateMs/dueDateMs (datepicker "YYYY-MM-DD" → epoch ms at midnight in `timezone` via luxon, or null when cleared). unresolvedAssignees is cleared to [] when the human has explicitly set asignados.
  </behavior>
  <action>Create src/slack/modal.ts. Import CLIENTS, MEMBERS, luxon DateTime. Export buildEditModal and parseEditSubmission plus the constants EDIT_CALLBACK_ID="edit_modal_submit" and stable block_ids/action_ids for each input (so parseEditSubmission reads them deterministically). Build option lists from CLIENTS/MEMBERS. For dates use DateTime.fromMillis(ms,{zone}).toFormat("yyyy-LL-dd") for initial_date and DateTime.fromFormat(value,"yyyy-LL-dd",{zone}).toMillis() when parsing. In modal.test.ts: assert buildEditModal embeds pendingId/channel/messageTs in private_metadata, offers exactly 7 Cliente + 9 Asignados options, and prefills initial values from a resolved task; assert parseEditSubmission round-trips a constructed view-submission payload into the correct patch (clienteOptionId UUID, numeric assigneeIds, epoch-ms dates in the team TZ, blank description→null, cleared date→null).</action>
  <verify>
    <automated>npx vitest run src/slack/modal.test.ts && npm run typecheck</automated>
  </verify>
  <done>buildEditModal/parseEditSubmission are pure and offline-tested for prefill + correct patch extraction (UUIDs, member ids, epoch-ms dates).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Edit open + submit handlers, re-render preview</name>
  <files>src/slack/interactions.ts, src/slack/interactions.test.ts</files>
  <behavior>
    - handleEditOpen(deps, {pendingId, channel, messageTs, triggerId}): getPending(pendingId) → if present, slack.views.open(triggerId, buildEditModal(resolved, {pendingId, channel, messageTs, timezone})). If the pending is gone (expired), no-op (or open a minimal "expiró" notice — keep minimal).
    - handleEditSubmit(deps, view): parseEditSubmission(view) → merge patch over the stored PendingTask.resolved → putPending(updated) → slack.chat.update(meta.channel, meta.messageTs, buildPreviewBlocks(meta.pendingId, updatedResolved, {timezone})) so the thread preview reflects the corrections (still showing Confirmar/Editar/Cancelar).
    - Both handlers are injectable and offline-testable.
  </behavior>
  <action>Extend src/slack/interactions.ts with handleEditOpen and handleEditSubmit over the existing injected deps (add slack.views.open to the structural slack type). Merge the parsed patch onto the existing resolved task (spread patch over resolved; only override fields present in the patch). In interactions.test.ts add: handleEditOpen with a seeded pending calls views.open with a view whose private_metadata carries the ids; handleEditSubmit applies a patch (e.g. sets a previously-null clienteOptionId and changes the due date) and asserts putPending stored the merged resolved AND chat.update re-rendered the preview blocks with the new values.</action>
  <verify>
    <automated>npx vitest run src/slack/interactions.test.ts && npm run typecheck</automated>
  </verify>
  <done>Edit open prefills from pending; submit corrects the pending and re-renders the preview; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Register edit action + view in Bolt app</name>
  <files>src/slack/app.ts</files>
  <action>In src/slack/app.ts register app.action("edit_task", ...) and app.view("edit_modal_submit", ...). In the edit_task action: await ack() then immediately call handleEditOpen with triggerId=body.trigger_id, pendingId from the action value, and channel/messageTs from body.container — open the modal promptly so the trigger_id stays valid (<3s). In the view handler: await ack() then call handleEditSubmit(deps, body.view). Reuse the same injected deps (redis, slack client wrapping Bolt `client` with chat.update/postMessage/views.open, timezone from env). No changes to api/slack/interactions.ts — the existing endpoint already routes view/action payloads through the same app.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/slack</automated>
  </verify>
  <done>edit_task action opens the modal within the trigger window; edit_modal_submit re-renders the preview; full src/slack suite + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack modal submit → service | Untrusted view_submission payload; values must map only to known config ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | modal field values | mitigate | Cliente/Asignados come from fixed static_select options (config UUIDs/ids); parseEditSubmission maps selected values back to the known config space, never free text |
| T-03-12 | Denial of Service | trigger_id expiry | mitigate | views.open is called right after ack() so the modal opens within Slack's ~3s trigger window |
| T-03-13 | Repudiation | edit on expired pending | accept | If the pending TTL lapsed, edit is a no-op; human re-sends the message (low impact, 1h window) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages (reuses luxon + existing deps) |
</threat_model>

<verification>
- `npx vitest run src/slack` and `npm run typecheck` green.
- modal.test proves prefill + patch extraction; interactions.test proves submit re-renders the corrected preview.
- Live modal interaction is human-deferred (needs deployed app + tokens).
</verification>

<success_criteria>
CONFIRM-04 is complete: Editar opens a modal with selects/pickers to correct cliente/asignados/fechas, and submitting updates the pending and re-renders the preview so the human can then Confirm the corrected task.
</success_criteria>

<output>
Create `.planning/phases/03-confirm-create/03-04-SUMMARY.md` when done.
</output>
