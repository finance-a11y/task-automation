---
phase: 02-nl-parser-resolver
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/resolve/cliente.ts
  - src/resolve/cliente.test.ts
  - src/resolve/assignees.ts
  - src/resolve/assignees.test.ts
  - src/resolve/dates.ts
  - src/resolve/dates.test.ts
  - src/resolve/index.ts
  - src/resolve/index.test.ts
  - src/resolve/types.ts
autonomous: true
requirements: [PARSE-02, PARSE-03, PARSE-04]

must_haves:
  truths:
    - "A cliente string (name or alias, case-insensitive) resolves to one of the 7 option UUIDs, or null when there is no confident match"
    - "Assignee names resolve to real member ids via name/alias + the Slack→member map; unmatched names are dropped and surfaced in unresolvedAssignees"
    - "Relative Spanish dates resolve to correct epoch milliseconds in TEAM_TIMEZONE for a fixed injected `now`, or null when unparseable"
    - "resolveTask is a pure function (no network, no process.env) returning a ClickUp-ready ResolvedTask"
  artifacts:
    - path: "src/resolve/cliente.ts"
      provides: "resolveCliente(raw, now?) → option UUID | null"
      exports: ["resolveCliente"]
    - path: "src/resolve/assignees.ts"
      provides: "resolveAssignees(rawNames, opts) → { ids, unresolved }"
      exports: ["resolveAssignees"]
    - path: "src/resolve/dates.ts"
      provides: "resolveSpanishDate(phrase, now, timezone) → epoch ms | null"
      exports: ["resolveSpanishDate"]
    - path: "src/resolve/index.ts"
      provides: "resolveTask(parsed, now, opts) → ResolvedTask"
      exports: ["resolveTask"]
  key_links:
    - from: "src/resolve/cliente.ts"
      to: "src/config/clients.ts"
      via: "import CLIENTS + CLIENT_ALIASES"
      pattern: "from \"\\.\\./config/clients"
    - from: "src/resolve/dates.ts"
      to: "luxon DateTime in TEAM_TIMEZONE"
      via: "DateTime.fromMillis(now, { zone })"
      pattern: "luxon"
---

<objective>
Build the deterministic resolver: pure functions that map the LLM's raw human strings to real ClickUp IDs and epoch-ms dates, leaving anything unmatched as null. This is the highest-value, fully-offline-testable core of the phase (Pitfalls 4 & 5: never trust the LLM's IDs, never let date math drift across timezones).

Purpose: resolve-then-confirm — the preview (phase 3) shows resolved values, so resolution correctness is load-bearing for the whole product.
Output: `src/resolve/{cliente,assignees,dates,index,types}.ts` with exhaustive unit tests. No network, no API key required to run any of it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-nl-parser-resolver/02-CONTEXT.md
@.planning/phases/02-nl-parser-resolver/02-01-SUMMARY.md

# Conventions: pure DI style + Vitest patterns to mirror
@src/store/redis.ts
@src/slack/process.test.ts

<interfaces>
<!-- From plan 01 (src/config/clients.ts, src/config/members.ts): -->
- CLIENTS: Record<ClientName, string>  (canonical name → option UUID)
- CLIENT_ALIASES: Record<string, ClientName>  (lowercase alias → canonical name)
- CLIENTE_FIELD_ID: string
- MEMBERS: Record<MemberName, number>  (canonical name → member id)
- MEMBER_ALIASES: Record<string, MemberName>
- SLACK_TO_MEMBER: Record<string, number>  (Slack user id → member id; empty by default)

<!-- ParsedTask shape this resolver consumes (defined in plan 03; declare the type here in types.ts so plans 02 and 03 share one contract): -->
ParsedTask = {
  title: string;
  description: string | null;
  clienteRaw: string | null;
  assigneesRaw: string[];
  startDatePhrase: string | null;
  dueDatePhrase: string | null;
  links: string[];
}
ResolvedTask = {
  title: string;
  description: string | null;
  clienteOptionId: string | null;
  assigneeIds: number[];
  unresolvedAssignees: string[];
  startDateMs: number | null;
  dueDateMs: number | null;
  links: string[];
}
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Shared types + cliente & assignee resolvers</name>
  <files>src/resolve/types.ts, src/resolve/cliente.ts, src/resolve/cliente.test.ts, src/resolve/assignees.ts, src/resolve/assignees.test.ts</files>
  <behavior>
    cliente.test.ts (PARSE-02):
    - Exact canonical name match (case-insensitive, trimmed): "FHCA"/"fhca"/" Fhca " → dce8df41-786e-40f4-9427-e833daf2d6a0.
    - Alias match: "feli" → Felipe Vergara's UUID; "nicmafia" → Delta/Nicmafia's UUID.
    - No confident match: "Cliente X", "", null → returns null (never invents).
    assignees.test.ts (PARSE-03):
    - Name match (case-insensitive): ["Veronica Romero"] → ids [118065209], unresolved [].
    - Alias match: ["vero"] → [118065209].
    - Slack→member override: a raw token that is a Slack user id present in SLACK_TO_MEMBER resolves via the map (inject a fixture map, since the real one is empty).
    - Unmatched dropped + surfaced: ["Veronica Romero","Pepe"] → ids [118065209], unresolved ["Pepe"].
    - Dedup: the same member referenced twice yields one id.
  </behavior>
  <action>
    Create src/resolve/types.ts exporting the ParsedTask and ResolvedTask types from the interfaces block (single shared contract for plans 02 + 03).
    Implement resolveCliente(raw: string | null): string | null in src/resolve/cliente.ts — normalize (trim, lowercase), match against CLIENTS keys (lowercased) then CLIENT_ALIASES; return the option UUID or null. RED→GREEN→REFACTOR; commit per cycle.
    Implement resolveAssignees(rawNames: string[], opts?: { slackToMember?: Record<string, number> }) → { ids: number[]; unresolved: string[] } in src/resolve/assignees.ts — for each raw token: try the Slack→member map (opts.slackToMember ?? SLACK_TO_MEMBER), then MEMBERS by name (case-insensitive), then MEMBER_ALIASES; collect resolved ids (deduped, order-stable), push unmatched raw strings to `unresolved`. Inject the slackToMember map so tests don't depend on the empty production default.
    Pure functions only — no I/O.
  </action>
  <verify>
    <automated>npm run test -- src/resolve/cliente.test.ts src/resolve/assignees.test.ts && npm run typecheck</automated>
  </verify>
  <done>cliente + assignee resolvers pass all cases above; types.ts exports the shared contracts; tsc clean.</done>
</task>

<task type="tdd">
  <name>Task 2: Spanish relative-date resolver (TZ-correct, epoch ms)</name>
  <files>src/resolve/dates.ts, src/resolve/dates.test.ts</files>
  <behavior>
    dates.test.ts (PARSE-04) — all with a fixed injected `now` and zone "America/Caracas" (UTC-4, no DST), asserting EXACT epoch ms:
    - "hoy" → start-of-day (or same-day) ms in zone for the `now` date.
    - "mañana" → now's calendar day + 1, resolved in zone.
    - "pasado mañana" → now + 2 days.
    - "viernes" / "el viernes" / "este viernes" → the next Friday on/after now, in zone.
    - "en 3 días" → now + 3 days.
    - Explicit "12/07" (dd/mm) → that date in the current year, in zone.
    - Off-by-one guard: a `now` set to 23:30 local (03:30 UTC next day) — "mañana" must still resolve to the correct LOCAL next day, NOT the UTC-shifted day (Pitfall 5).
    - Unparseable ("algún día", "", null) → null.
    Document the chosen time-of-day convention (start-of-day vs noon) in a comment and assert it consistently; whichever is chosen, the ms value is computed in-zone then converted to a UTC epoch.
  </behavior>
  <action>
    Implement resolveSpanishDate(phrase: string | null, now: number, timezone: string): number | null in src/resolve/dates.ts using luxon. Build `DateTime.fromMillis(now, { zone: timezone })`, normalize the phrase (lowercase, strip articles "el"/"este"/"próximo"/accents on weekday names), and branch: hoy / mañana / pasado mañana / weekday names (lunes..domingo → next occurrence on-or-after today) / "en N días" / explicit dd/mm (and dd/mm/yyyy). Set a single consistent time-of-day (Claude's Discretion: start-of-day is simplest and matches all-day ClickUp tasks) via `.startOf("day")`, then return `.toMillis()` (a true UTC epoch — luxon handles the zone offset). Return null for anything unmatched. `now` is injected for determinism (never call Date.now()). RED→GREEN→REFACTOR; commit per cycle.
  </action>
  <verify>
    <automated>npm run test -- src/resolve/dates.test.ts && npm run typecheck</automated>
  </verify>
  <done>All date cases pass including the 23:30-local off-by-one guard; null on unparseable; no Date.now() in source.</done>
</task>

<task type="tdd">
  <name>Task 3: resolveTask aggregator</name>
  <files>src/resolve/index.ts, src/resolve/index.test.ts</files>
  <behavior>
    index.test.ts:
    - A fully-populated ParsedTask resolves to a ResolvedTask with clienteOptionId set, assigneeIds populated, startDateMs/dueDateMs computed, links + title/description passed through.
    - All-null/empty fields resolve to clienteOptionId=null, assigneeIds=[], date fields=null, unresolvedAssignees reflecting any unmatched names.
    - timezone is taken from an injected option (default "America/Caracas"); `now` injected.
    - Pure: calling twice with the same inputs yields deeply-equal output.
  </behavior>
  <action>
    Implement resolveTask(parsed: ParsedTask, now: number, opts?: { timezone?: string; slackToMember?: Record<string, number> }): ResolvedTask in src/resolve/index.ts. Compose resolveCliente, resolveAssignees, resolveSpanishDate (startDatePhrase → startDateMs, dueDatePhrase → dueDateMs). Pass title/description/links through unchanged. Default timezone to "America/Caracas" (the env default — the caller in plan 03 will pass env.TEAM_TIMEZONE). Re-export the resolver functions and types from this module as the resolve/ barrel.
  </action>
  <verify>
    <automated>npm run test -- src/resolve/index.test.ts && npm run typecheck</automated>
  </verify>
  <done>resolveTask composes all three resolvers into a ResolvedTask; pure; default timezone applied; barrel re-exports in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output (ParsedTask) → resolver | The raw strings are untrusted/hallucination-prone; the resolver is the validation gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | resolveCliente / resolveAssignees | mitigate | Only return IDs that exist in the fixed CLIENTS/MEMBERS maps; any non-match → null/unresolved, never an invented ID (Pitfall 4) |
| T-02-04 | Tampering | resolveSpanishDate timezone math | mitigate | All arithmetic done in TEAM_TIMEZONE via luxon with injected `now`; explicit 23:30-local off-by-one test locks correctness (Pitfall 5) |
</threat_model>

<verification>
- `npm run test` green across all src/resolve/* suites.
- `npm run typecheck` clean.
- Purity: `grep -rn "Date.now\|process.env\|fetch(" src/resolve` returns nothing.
</verification>

<success_criteria>
- cliente → option UUID or null (PARSE-02).
- assignees → member ids + unresolved list (PARSE-03).
- Spanish relative dates → correct epoch ms in team TZ or null (PARSE-04).
- resolveTask is pure and composes all three.
</success_criteria>

<output>
Create `.planning/phases/02-nl-parser-resolver/02-02-SUMMARY.md` when done.
</output>
