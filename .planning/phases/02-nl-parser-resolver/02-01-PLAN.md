---
phase: 02-nl-parser-resolver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/config/env.ts
  - src/config/env.test.ts
  - src/config/clients.ts
  - src/config/clients.test.ts
  - src/config/members.ts
  - src/config/members.test.ts
autonomous: false
requirements: [PARSE-01, PARSE-02, PARSE-03]
user_setup:
  - service: openai
    why: "NL extraction of task fields from Spanish Slack messages (PARSE-01). Only needed for the live smoke test in plan 03 and for production; all offline tests inject a mock client."
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard → API keys (https://platform.openai.com/api-keys)"

must_haves:
  truths:
    - "loadEnv requires OPENAI_API_KEY and exposes OPENAI_MODEL with default gpt-4o-mini"
    - "The 7 Cliente dropdown options resolve name/alias → exact option UUID from config-as-code"
    - "The 9 ClickUp members resolve name/alias → exact numeric member id, with a structured Slack-userID→member-id override map"
  artifacts:
    - path: "src/config/clients.ts"
      provides: "Typed CLIENTS constant (name → option UUID) + alias table + CLIENTE_FIELD_ID"
      contains: "05ebdc8a-4736-404d-9132-3ab32875e1f1"
    - path: "src/config/members.ts"
      provides: "Typed MEMBERS constant (name → member id) + alias table + Slack→member override scaffold"
      contains: "216158839"
    - path: "src/config/env.ts"
      provides: "Extended env contract with OPENAI_API_KEY + OPENAI_MODEL"
      contains: "OPENAI"
  key_links:
    - from: "src/config/clients.ts"
      to: "Cliente dropdown field 05ebdc8a-4736-404d-9132-3ab32875e1f1"
      via: "exported CLIENTE_FIELD_ID constant"
      pattern: "05ebdc8a-4736-404d-9132-3ab32875e1f1"
---

<objective>
Lay the config-as-code + env foundation the parser and resolver ride on: extend the env contract with the OpenAI vars, install the OpenAI SDK and a timezone-correct date library, and encode the 7 real Cliente option UUIDs and 9 real ClickUp member IDs (with alias tables) as typed constants.

Purpose: The resolver (plan 02) maps LLM-emitted human strings to real ClickUp IDs against these maps; the parser (plan 03) needs the OpenAI client/model from env. Both are useless without correct, validated IDs — so the IDs live in version control with unit tests asserting their exact values (Pitfall 4/6: never invent or mistype an ID).
Output: Updated `package.json`/`.env.example`/`env.ts`, and new `src/config/clients.ts` + `src/config/members.ts` with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nl-parser-resolver/02-CONTEXT.md

# Phase 1 conventions to mirror (strict ESM, zod fail-fast env, lazy DI factory)
@src/config/env.ts
@src/config/env.test.ts
@src/store/redis.ts

<interfaces>
<!-- Existing env contract (extend, don't rewrite). From src/config/env.ts: -->
- `loadEnv(source = process.env): Env` — zod safeParse, throws naming every offending key.
- `nonEmpty = z.string().trim().min(1)`; `TEAM_TIMEZONE` already present with default `"America/Caracas"`.
- Pattern to mirror for the client factory: `createRedis(env)` in src/store/redis.ts is a lazy factory that throws if a required key is empty — never reads process.env at module load.

<!-- Real IDs (AUTHORITATIVE — copy exactly from 02-CONTEXT.md). -->
Cliente dropdown field id: 05ebdc8a-4736-404d-9132-3ab32875e1f1
Clientes (name → option UUID):
- Felipe Vergara   63d9626f-9b80-4a19-8638-93b8042d2e9c
- Children Chic    57123824-86d1-4fb8-a3a3-03fb1a8d8704
- Ultra1plus       b48a4350-8c92-434f-88d4-00527f2eb157
- FHCA             dce8df41-786e-40f4-9427-e833daf2d6a0
- Delta/Nicmafia   bf842969-d5c2-4eb8-a1fb-5d87d804eb0d
- Apturio          cde11ae3-2d92-4ca4-b9d7-ab4157af67ff
- Interno          c95d4707-50a8-4833-9046-9c153a4f7592
Members (name → ClickUp member id):
- Miguel Pacheco 216158839 · Juan Carlos Angulo 216178477 · Veronica Romero 118065209
- Amira El Sahli 112092886 · Oriana Reyes 106163644 · Fernando Perez 162145488
- Natalia Olivares 105901293 · Cammila Hernandez 100128182 · Arianna Lupi 150028631
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (runs BEFORE any install)</name>
  <what-built>Nothing yet — this gate runs before `npm install`. This plan installs three packages treated as [ASSUMED] under the Package Legitimacy Gate fallback (RESEARCH.md has no "## Package Legitimacy Audit" table). The `openai` package is mandated by the LOCKED CONTEXT decision; `luxon` + `@types/luxon` are Claude's-discretion choice for TZ-correct date math (alternative was date-fns + date-fns-tz). This mirrors the same gate Phase 1 used in 01-01.</what-built>
  <how-to-verify>
    Confirm each is the genuine official package on npmjs.com (correct publisher, healthy download counts, not a typosquat):
    1. https://www.npmjs.com/package/openai — publisher: openai (OpenAI's official Node SDK)
    2. https://www.npmjs.com/package/luxon — publisher: moment/Isaac Cambron (DefinitelyTyped-adjacent, multi-million weekly downloads)
    3. https://www.npmjs.com/package/@types/luxon — publisher: types (DefinitelyTyped)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name any package to swap/reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install deps + extend the env contract</name>
  <files>package.json, .env.example, src/config/env.ts, src/config/env.test.ts</files>
  <action>
    Run `npm install openai luxon` and `npm install -D @types/luxon` (latest, per LOCKED CONTEXT: `openai` SDK with structured outputs; luxon chosen for IANA-TZ-correct date math per Claude's Discretion).
    Extend EnvSchema in src/config/env.ts (do NOT rewrite the file — add fields and keep the existing fail-fast/no-process.env-at-load behavior, per PARSE-01): add `OPENAI_API_KEY: nonEmpty` (required — production fail-fast) and `OPENAI_MODEL: nonEmpty.default("gpt-4o-mini")`. Reuse the existing `nonEmpty` helper. TEAM_TIMEZONE already exists — do not touch it; it is reused by the date resolver in plan 02.
    Update src/config/env.test.ts: add OPENAI_API_KEY to the `valid()` fixture, add a test asserting OPENAI_MODEL defaults to "gpt-4o-mini" when absent, and a test that a missing OPENAI_API_KEY throws naming the key (mirror the existing missing-key test pattern).
    Append OPENAI_API_KEY and OPENAI_MODEL entries to .env.example with a one-line comment each, matching the existing comment style. OPENAI_MODEL comment notes the default + gpt-4.1-mini fallback.
    Do NOT add an OpenAI client factory here — that is plan 03 (keeps this plan's surface to env + config only).
  </action>
  <verify>
    <automated>npm run test -- src/config/env.test.ts && npm run typecheck</automated>
  </verify>
  <done>env.test passes including the new OPENAI_MODEL default + missing-key cases; tsc clean; openai + luxon present in package.json dependencies, @types/luxon in devDependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Config-as-code maps — clients.ts + members.ts</name>
  <files>src/config/clients.ts, src/config/clients.test.ts, src/config/members.ts, src/config/members.test.ts</files>
  <behavior>
    clients.test.ts:
    - CLIENTE_FIELD_ID === "05ebdc8a-4736-404d-9132-3ab32875e1f1".
    - CLIENTS has exactly 7 entries; each canonical name maps to the exact UUID listed in the interfaces block (assert all 7 verbatim).
    - CLIENT_ALIASES maps e.g. "feli"→"Felipe Vergara", "delta"→"Delta/Nicmafia", "nicmafia"→"Delta/Nicmafia" (alias values are canonical CLIENTS keys).
    members.test.ts:
    - MEMBERS has exactly 9 entries; assert all 9 name→id pairs verbatim (e.g. "Miguel Pacheco" → 216158839).
    - MEMBER_ALIASES maps e.g. "vero"→"Veronica Romero".
    - SLACK_TO_MEMBER is an exported, typed (Record<string, number>) override map — empty by default (Slack IDs unknown until the workspace is wired) but present so real IDs slot in later without code changes.
  </behavior>
  <action>
    Create src/config/clients.ts exporting: `CLIENTE_FIELD_ID` (string const), `CLIENTS` (a typed `Record<ClientName, string>` of the 7 canonical name→option-UUID pairs from the interfaces block, copied EXACTLY — PARSE-02), a `ClientName` union type derived from the keys, and `CLIENT_ALIASES` (Record<string, ClientName>) for fuzzy text matches (feli, delta, nicmafia, children, ultra, fhca, apturio, interno — pick reasonable lowercase aliases; Claude's Discretion on the exact list). Do NOT implement matching logic here — just the data; the resolver (plan 02) consumes it.
    Create src/config/members.ts exporting: `MEMBERS` (typed `Record<MemberName, number>` of the 9 name→id pairs, copied EXACTLY — PARSE-03), `MemberName` union type, `MEMBER_ALIASES` (Record<string, MemberName>, e.g. vero, juancarlos/jc, ari, cammi — Claude's Discretion), and `SLACK_TO_MEMBER: Record<string, number>` seeded empty with a comment explaining it is the env-or-config override slot for Slack-userID→member-id (per CONTEXT specifics — structure so real Slack IDs slot in later).
    Keep both files pure data + types, no I/O, no process.env reads.
  </action>
  <verify>
    <automated>npm run test -- src/config/clients.test.ts src/config/members.test.ts && npm run typecheck</automated>
  </verify>
  <done>Both config files exist with exact IDs verified by tests; alias tables present; SLACK_TO_MEMBER scaffold exported; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party package code enters the supply chain at install |
| config-as-code → resolver/ClickUp | These IDs become the only source of truth for what gets written to ClickUp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (openai, luxon, @types/luxon) | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies each on npmjs.com before install; [ASSUMED] fallback (no audit table) |
| T-02-01 | Tampering | clients.ts / members.ts ID constants | mitigate | Unit tests assert every UUID/member id verbatim against 02-CONTEXT.md — a mistyped ID fails the build, not silently mis-tasks (Pitfall 6) |
| T-02-02 | Information disclosure | OPENAI_API_KEY in env | accept | Env-var only (never committed); .env.example holds a placeholder; key is not logged anywhere in this plan |
</threat_model>

<verification>
- `npm run test` green for env, clients, members suites.
- `npm run typecheck` clean.
- Grep confirms the Cliente field id literal is present: `grep -v '^[[:space:]]*//' src/config/clients.ts | grep -c "05ebdc8a-4736-404d-9132-3ab32875e1f1"` ≥ 1.
</verification>

<success_criteria>
- loadEnv requires OPENAI_API_KEY and defaults OPENAI_MODEL to gpt-4o-mini (PARSE-01 foundation).
- The 7 Cliente UUIDs and 9 member IDs are encoded exactly and test-locked (PARSE-02/03 foundation).
- Alias tables + SLACK_TO_MEMBER override scaffold exist for the resolver to consume.
</success_criteria>

<output>
Create `.planning/phases/02-nl-parser-resolver/02-01-SUMMARY.md` when done.
</output>
